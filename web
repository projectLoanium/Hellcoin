import React, { useState } from "react";
import { motion } from "framer-motion";
import "./FlameAnimation.css";

const HellCoin = () => {
  const [burned, setBurned] = useState(false);

  const handleBurn = () => {
    setBurned(true);
  };

  return (
    <div className="min-h-screen bg-black text-white flex flex-col items-center justify-center relative overflow-hidden">
      <motion.img 
        src="/mnt/data/photo_2025-03-31_23-17-41-removebg-preview.png"
        alt="Hellcoin Logo"
        className={`w-96 h-96 mb-10 relative z-10 ${burned ? 'opacity-0 transition-opacity duration-1000' : ''}`}
        initial={{ scale: 0.8, opacity: 0 }}
        animate={{ scale: 1, opacity: 1 }}
        transition={{ duration: 0.8 }}
      />
      
      <div className="flame-container absolute top-0 left-0 w-full h-full z-0">
        <div className="flame red"></div>
        <div className="flame yellow"></div>
        <div className="flame blue"></div>
        <div className="flame green"></div>
      </div>
      
      <motion.h1 
        className={`text-6xl font-bold mb-10 relative z-10 ${burned ? 'text-red-600' : ''}`}
        initial={{ scale: 0.8, opacity: 0 }}
        animate={{ scale: 1, opacity: 1 }}
        transition={{ duration: 0.8 }}
      >
        {burned ? 'WELCOME TO HELL 🔥' : 'HELLCOIN'}
      </motion.h1>
      
      <motion.p 
        className={`text-xl text-center max-w-3xl relative z-10 mb-8 ${burned ? 'opacity-0 transition-opacity duration-1000' : ''}`}
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ duration: 1.5 }}
      >
        {burned ? 'Your soul has been burned. Enjoy your stay in the underworld.' : 'The ultimate cryptocurrency forged in fire and skulls. Join the revolution and claim your spot in the underworld of finance.'}
      </motion.p>
      
      <motion.button 
        className={`px-8 py-4 ${burned ? 'bg-red-800' : 'bg-red-600 hover:bg-red-800'} rounded-2xl text-xl relative z-10`}
        whileHover={{ scale: 1.1 }}
        whileTap={{ scale: 0.9 }}
        onClick={handleBurn}
      >
        {burned ? 'You are in Hell 🔥' : 'Burn to Enter Hell'}
      </motion.button>
    </div>
  );
};

export default HellCoin;
